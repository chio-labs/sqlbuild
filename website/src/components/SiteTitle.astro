---
import logoDark from '../assets/logo-dark.png';
import logoLight from '../assets/logo-light.png';
---

<div class="site-title">
	<a class="logo" href="/" aria-label="SQLBuild home">
		<img class="logo-for-light" src={logoLight.src} alt="SQLBuild" width={111} height={30} />
		<img class="logo-for-dark" src={logoDark.src} alt="SQLBuild" width={111} height={30} />
	</a>
	<span class="divider" aria-hidden="true"></span>
	<a class="docs" href="/docs/">Docs</a>
</div>

<style>
	.site-title {
		display: flex;
		flex-shrink: 0;
		align-items: center;
		gap: 12px;
		min-width: 0;
	}

	.logo {
		display: flex;
	}

	img {
		width: auto;
		height: 30px;
	}

	.logo-for-dark,
	:global(:root[data-theme='dark']) .logo-for-light {
		display: none;
	}

	:global(:root[data-theme='dark']) .logo-for-dark {
		display: block;
	}

	.divider {
		width: 1px;
		height: 20px;
		background: var(--sqb-line-strong);
	}

	.docs {
		color: var(--sqb-fg);
		font-size: 16px;
		font-weight: 550;
		text-decoration: none;
	}

	.docs:hover {
		color: var(--sqb-primary);
	}
</style>
