name: waffle_shop
adapter: duckdb

connection:
  database: waffle_shop.duckdb

settings:
  default_audit_severity: warn

defaults:
  materialized: table

path_defaults:
  models/staging:
    materialized: view
