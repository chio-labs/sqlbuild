name = "dbt_interop_demo"
adapter = "duckdb"

[settings]
default_audit_severity = "warn"

[connection]
database = "dbt_interop.duckdb"

[dbt]
project_dir = "../dbt_project"
profiles_dir = "../profiles"
target_path = "../dbt_project/target"
